import Container from '@/components/Container';

const ServicesPage = () => {
  return (
    <Container
      layout="blog"
      title="Services | Semesta Biz"
      description="Wordpress Web Design"
      // date={new Date(frontMatter.publishedAt).toISOString()}
      type="article"
      fullWidth={true}
    >
test
    </Container>
  )
}

export default ServicesPage
